import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
  // styles: [` h3 { color: violet; }`]  //can use either way as styles in an array, or linking to the css folder
})

export class AppComponent {
  
}
